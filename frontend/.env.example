# Local development can use the Vite proxy by leaving this unset.
# For direct production API access, set this to the deployed Spring Boot API URL + /api.
# Example: https://capstone-dairy-backend.onrender.com/api
VITE_API_BASE_URL=
